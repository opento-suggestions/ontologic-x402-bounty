{
  "name": "witness-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Witness Required — goose plugin (MCP server, stdio). The payer-agent's driver: assert a claim, meet Payment Required, pay through x402, stamp your own testimony.",
  "bin": {
    "witness-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.72.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
